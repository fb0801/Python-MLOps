

services:
  jupyter:
    build: .
    restart: unless-stopped
    ports:
      - "8888:8888"
    volumes:
      - .:/home/jovyan/work
    command: >
      start-notebook.sh
      --NotebookApp.token=''
      --NotebookApp.password=''

